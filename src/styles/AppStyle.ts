import styled from "styled-components";

export const Container = styled.main`
  width: 100%;
  max-width: 1200px;

  form {
    margin: 1rem;
    display: flex;
    flex-direction: column;
  }

  input {
    margin: 0.5rem 0;
  }

  button {
    margin: 0.5rem 0;
  }
`;
